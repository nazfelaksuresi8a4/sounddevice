import sounddevice as sd 
import numpy as np 

channels = 1
duration = 6
samplerate = 44100

recording = sd.rec(
    int(duration * samplerate),
    samplerate=samplerate,
    channels=channels,
    dtype='float32',
)
sd.wait()

sd.play(recording,samplerate)
sd.wait()
